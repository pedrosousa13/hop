//! The launcher window's widget tree: the pre-built [`window::HopWindow`],
//! the item [`model`] backing its results list, and the recycling
//! [`row`] factory that draws it.

pub mod model;
pub mod row;
pub mod window;
